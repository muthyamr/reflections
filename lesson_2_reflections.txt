What happens when you initialize a repository? Why do you need to do it?
It instialize git repository in that directory, by using of this git repository we can track all commits placed in that directory

How is the staging area different from the working directory and the repository? What value do you think it offers?
not sure exactly, repository stores only commited information, working directory contains all files.
